# Installation

Download and create a new project with:

```bash
curl -fsSL https://raw.githubusercontent.com/alirizagurtas/agent-template/main/install.sh | bash -s -- my-project my_package my-project
```

The arguments are destination directory, Python package name, and distribution
name. When no arguments are supplied, the installer prompts for them when a
terminal is available.
